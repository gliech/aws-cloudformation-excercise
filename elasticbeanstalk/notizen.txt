sind S3 bucket urls die in application version definitions angegeben werden der ständige speicherort der zip files, und werden diese auch aus diesem s3 bucket gelöscht wenn die lifecycle policy das verlangt, oder werden sourcebundles immer in den applikationseigenen bucket geladen?


Wie dumm sind die recommended values der configuration options?
Grafik einbauen, die den configuration option precedence zeigt, linear und dann aufspalten in console, cli, und andere, mit recommended values direkt dadrunter gehängt


command und container_command unterschied
saved configurations und configuration templates sind das gleiche
solution stack und platform sind das gleiche?
unbeding configuration option precedence erwähnen
erwähnen, dass das system so gedacht ist, dass applikationen speziell für die anwendung in  beanstalk geschrieben werden sollen
 - SQS worker
 - beste art environment spezifische konfiguration einzubringen ist bei wordpress $_SERVER

am ende die eb-cli erwähnen
 - zeigen welche befehle es bräuchte eine neue environment auszurollen
 - gut wenn man entwicklern große kontrolle gibt
 - nicht gut für production environments die man vllt zusammen mit einem zugehörigen netzwerkstack deployen möchte

Applications
- Container für Application Versions

http://elasticbeanstalk-env-resources-eu-west-1.s3.amazonaws.com/eb_snippets/rds/rds.json

extra fn::
